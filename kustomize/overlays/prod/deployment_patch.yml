apiVersion: apps/v1
kind: Deployment
metadata:
  name: ssslite-deployment
spec:
  template:
    spec:
      containers:
        - name: ssslite
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: ssslite-env-prod
